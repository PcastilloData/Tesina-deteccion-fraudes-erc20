# Librerías base y manipulación de datos
pandas>=2.0.0
numpy>=1.20.0
scikit-learn>=1.4.0
imblearn>=0.12.2
requests>=2.28.0
matplotlib>=3.7.0
seaborn>=0.13.0
Pillow # Si se usa para gráficos (aunque no se importa explícitamente, es buena práctica si se guarda como imagen)

# Librerías para Modelado Avanzado
xgboost>=1.6.0
lightgbm>=4.3.0

# Librerías específicas para GCP y BigQuery
google-cloud-bigquery>=3.0.0

# Para guardar/cargar modelos (usado en preprocesamiento y GUI)
# pickle es nativo de Python, pero el soporte de archivos es a través de las librerías base.

# Para la Interfaz Gráfica (Tkinter es nativo, pero se incluye por completitud)
# tkinter (nativo de Python)
